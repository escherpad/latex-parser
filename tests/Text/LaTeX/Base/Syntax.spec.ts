import "mocha";
import {expect} from "chai";

describe("Syntax", () => {
    it("getBody", () => {
        // TODO
        // expect(JSON.stringify(getBody(
        //         newTeXEnv("document", [{}], newFixArg([{}]))
        //     ))).to.equal("");
    });
});